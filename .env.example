# Optional. Without a key the marketplace runs in full: natural-language search
# falls back to the rules parser and the message drafter is hidden.
ANTHROPIC_API_KEY=

# Optional. Defaults to ./data/n5deal.db
# DATABASE_URL=./data/n5deal.db
